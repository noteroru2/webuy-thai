<IfModule mod_rewrite.c>
  RewriteEngine On

  # Force trailing slash to match Astro's "trailingSlash: 'always'" config
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_URI} !/$
  RewriteRule ^(.*)$ /$1/ [L,R=301]
</IfModule>

# Handle 404
ErrorDocument 404 /404.html
